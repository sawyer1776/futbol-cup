/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'media-4.api-sports.io', 'media.api-sports.io'],
  },
  // show me an example of setting the imgaes domain in next config

}

module.exports = nextConfig
